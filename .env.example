# Image tag to deploy — CI overwrites this with the commit SHA on each deploy.
# DOCKER_USERNAME (used as the image namespace) is set further down.
IMAGE_TAG=latest

# Postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me
POSTGRES_DB=automation

# Backend runtime
NODE_ENV=production
PORT=3001
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
CORS_ORIGIN=https://your-frontend-domain.example
DOCKER_USERNAME=alialahmad
DOCKER_PASSWORD=dckr_pat_S_tStqQDDMp83FA1Pcukn9_TURM
SERVER_HOST=100.31.88.42
SERVER_USER=ubuntu
SERVER_SECRET=your-ssh-secret

# Frontend — baked into the client bundle at image build time via GH Actions build-arg.
# Present here for reference and for local docker compose builds.
NEXT_PUBLIC_API_BASE_URL=https://your-backend-domain.example/api
